services:
  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - sse_mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: sse_market

volumes:
  sse_mongo_data:
